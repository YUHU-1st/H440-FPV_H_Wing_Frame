# B1 交付状态

H440 B1 当前数字制造交付已经完成重生成与核验。参数和几何源文件仍为工作目录中的 `build_h440_b1.py`；SolidWorks 文件是由当前 STEP 主模型生成的下游原生交付文件，不是手工重建的参数化特征树。

## 已完成

- B1 全量 STEP/STL/DXF 重新生成；当前零件实体干涉为 0。
- 所有零件 STEP round-trip 有效；7 份碳板 DXF 均已写入 mm 单位。
- 假定 78×48×52 mm 电池在三个代表性纵向位置的机架体积干涉均为 0。
- DJI O4 Pro 相机左右可调侧板已进入主模型；0–90° 以 0.25° 步进共 361 个姿态完成包络和光轴检查，碰撞数为 0，最小电子板净空约 2.128 mm。
- SOLIDWORKS Premium 2024 SP5.0（revision 32.5.0）中，28 个当前组件 STEP 均已保存为 `SLDPRT`，随后关闭并静默重开，未报告打开错误或警告。
- `H440_B1_FRAME.SLDASM` 已由当前 FRAME STEP 重新生成。SolidWorks 导入结构为 1 个根包装子装配 + 43 个生成实例；全部 44 个引用均持久化到仓库内文件，关闭全部文档后重新打开总装，errors=0、warnings=0，且不存在 `Temp/IC~~` 临时引用。详细记录见 `solidworks_native_report.json`。
- 碳板供应商 DXF/STEP 交付包见 `H440_B1_supplier_DXF_STEP.zip`，下料口径见 `供应商交付说明.md`。

## 当前重量口径

CAD 结构净重约 **404.23 g**；紧固件、绑带、防滑垫和胶估算约 **125.92 g**；机架合计预算约 **530.15 g**。这些数值仍属于 CAD/预算结果，首件完成后应以实测称重更新。

## 尚需实物验证

大黄狗 6S 1600 mAh 电池与具体电机、飞塔等实物尺寸仍需装机复核。结构承载、胶接与嵌件强度、振动、动力系统、飞控混控/转换和飞行性能均尚未完成实物验证；数字几何核验通过不能替代这些测试。
